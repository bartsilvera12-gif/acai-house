import GerenciaClient from "./GerenciaClient";

/** Módulo Gerencia: tablero comercial (revenue, MRR, cuentas por cobrar, recurrentes). */
export default function GerenciaPage() {
  return <GerenciaClient />;
}
